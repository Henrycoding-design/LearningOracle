# Learning Oracle Plan

We have several things:

* markdown handling
* Better UI (probably remove the texts on the sidebar and add the tooltips instead)
* Review the codes API in gemini.ts
* Add one more feature: YouTube vid recommendation, Illustration graph for learning
* Consider add 'stepfun-3.5-flash' openrouter API to improve latency
* Consider stream mode instead (if available) -> ChatGPT like text generation, so less waiting time

